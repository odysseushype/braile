# Ambiente Python
.venv/
venv/
__pycache__/
*.pyc

# Dados/estado/config gerados em tempo de execução (contêm caminhos internos
# da rede da empresa e resultados de produção — não versionar)
vigia_braile_config.json
vigia_braile_estado.json
vigia_braile.log
vigia_braile_resultado.csv
vigia_braile_crash.log

# Build local do PyInstaller
build/
dist/
*.spec

# Pasta local de teste com imagens de amostra (dados reais de produção —
# nomes de cliente/produto — não versionar)
vigia_braile_teste/
brailes/

.DS_Store
